interface:
  display_name: "Apply Ledger Changes"
  short_description: "Review candidate ledger edits and posting changes"
  default_prompt: "Use $auto-bean-apply to turn reviewed parsed statement evidence into candidate Beancount postings or review another structural ledger change, keep parsed statement facts separate from derived ledger edits, show the validation outcome and git diff, and ask before commit or push."

policy:
  allow_implicit_invocation: true
