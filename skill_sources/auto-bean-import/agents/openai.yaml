interface:
  display_name: "Import Statements"
  short_description: "Parse statements and propose first-seen accounts"
  default_prompt: "Use $auto-bean-import to normalize statement files into `statements/parsed/`, track `statements/import-status.yml`, and prepare reviewable first-seen account proposals without mutating the ledger."

policy:
  allow_implicit_invocation: true
