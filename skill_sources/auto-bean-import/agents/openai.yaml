interface:
  display_name: "Import Statements"
  short_description: "Normalize raw statements with Docling"
  default_prompt: "Use $auto-bean-import to discover or re-parse statement files into `statements/parsed/` without mutating the ledger."

policy:
  allow_implicit_invocation: true
