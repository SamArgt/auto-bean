interface:
  display_name: "Import Statements"
  short_description: "Run the full statement import workflow"
  default_prompt: "Use $auto-bean-import to inspect raw statements, process only new or stale inputs, resolve process question artifacts, update intermediate statements as needed, derive first-seen accounts, apply reviewed postings, validate the ledger, and hand memory suggestions to $auto-bean-memory at the end."

policy:
  allow_implicit_invocation: true
