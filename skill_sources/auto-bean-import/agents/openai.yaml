interface:
  display_name: "Import Statements"
  short_description: "Parse statements and extend ledger account structure"
  default_prompt: "Use $auto-bean-import to normalize statement files into `statements/parsed/`, track `statements/import-status.yml`, derive direct first-seen account structure updates from imported evidence, validate the ledger, show a diff, and ask before commit or push."

policy:
  allow_implicit_invocation: true
